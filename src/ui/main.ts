// Browser entry point. Fleshed out once the engine exists; for now it just
// proves the toolchain wiring (index.html -> module -> #app).
const app = document.getElementById('app')
if (app) app.textContent = 'Tactics Sandbox — engine not yet wired.'
